buildscript {
    dependencies {
        classpath 'com.android.tools.build:gradle:7.0.0'
    }
    // Error: 'repositories' cannot be inferred here if the block is closed or malformed above
    repositories {
        google()
        mavenCentral()
    }
}
